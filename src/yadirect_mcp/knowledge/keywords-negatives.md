# Ключевые фразы, минус-слова, операторы

## Операторы

| Оператор | Что делает | Пример |
|---|---|---|
| `!` | фиксирует форму слова (число, падеж, время), не запрещает добавочные слова | `купить !собаку` фиксирует «собаку»; дополнительные слова сам оператор не исключает |
| `+` | фиксирует стоп-слово или служебную часть речи | `работа +на дому` ≠ «работа по дому» |
| `" "` | фиксирует количество слов, без добавочных | `"купить автомобиль"` — без «купить красный автомобиль» |
| `[ ]` | фиксирует порядок слов; словоформы и стоп-слова учитываются | `[дома на воде]` ≠ «вода на дом» |
| `( | )` | группирует альтернативы | `купить машину (недорого|ваз)` |
| `-` | исключает слово/фразу | `купить собаку -игрушка` |

Директ и сам учитывает влияние стоп-слов на смысл фразы и отсекает часть нерелевантных запросов без явных операторов.

## Минус-фразы: главное правило

Обязательного универсального списка минус-фраз нет. В MCP 1.5.0 используйте
`negative_keyword_policy`: профиль по нише, `business_terms`, дополнения и
исключения клиента с причиной. «Прокат» может быть целевой услугой, «урок» —
продуктом школы, «игрушка» — товаром, «бесплатная доставка» — частью предложения.
Сначала сверяйте кандидата с ассортиментом, объявлениями, посадочной и запросом;
затем принимайте решение. Локальная проверка пересечений — подсказка специалисту,
она не доказывает, что Директ исключит конкретный показ.

**Если все слова минус-фразы входят в ключевую фразу, минус-фраза игнорируется.** Ключевая `купить розового слона` + минус `купить слона` → минус не срабатывает, показы идут. Это ловушка при машинной генерации минусовки из отчёта по поисковым запросам: сгенерированный минус может быть просто проигнорирован, и «исправление» ничего не исправит.

Уровни минусовки: кампания, группа, отдельная ключевая фраза (минус-слова — на все три уровня, минус-фразы — на кампанию и группу). Плюс **общие наборы минус-фраз**: заводятся один раз и привязываются к группам, но ни в одном отчёте не видны. Что заведено и привязано ли — `direct_account_settings`, секция `negative_keyword_sets` (`associated` показывает привязку, длинные наборы приходят с количеством и началом списка).

**Кросс-минусовка делается автоматически:** если ключевые фразы пересекаются (одна содержит другую), Директ сам добавляет минус-слова, и фразы перестают конкурировать за показ. Вручную это делать не нужно.

Лимиты — см. «Технические ограничения Директа» (`direct://kb/tech-limits`): 7 слов во фразе, 200 фраз на группу, 20 000 символов минус-фраз на кампанию.

## Статус «Мало показов»

Ставится **группе**, а не фразе. Причины: ультранизкочастотные фразы, слишком узкий таргетинг, чрезмерно жёсткие минус-слова, заниженная ставка или понижающие корректировки.

Лечение: **объединять узкие и низкочастотные фразы в одну группу** (алгоритму проще обучаться и набирать показы), пересматривать выбранные регионы по согласованной аудитории, снимать лишние ограничения. Разбиение «одна фраза = одна группа» в современном Директе работает против вас.

## Подбор семантики

Частотность даёт тул `direct_wordstat`: до 50 фраз за вызов, регионы Директа в `geo_ids` (коды сверять через `direct_regions` — неверный Директ примет молча и посчитает спрос по чужому региону), порог отсечения в `min_shows`. Полный список подсказок уходит на диск и читается через `direct_read_report`, в ответе — сводка по каждой фразе.

**Как читать частотность.** Базовое число по фразе без операторов — это спрос по **широкому соответствию**: в него входят все запросы, содержащие эти слова в любых формах и с любыми добавками. «окна пвх» с 165 тысячами показов включает и «купить окна пвх», и «окна пвх бу», поэтому складывать фразу с её же вложенными запросами нельзя — это двойной счёт. Чтобы увидеть спрос именно по фразе: кавычки ограничивают её словами (`"окна пвх"`), `!` фиксирует словоформы (`!окна !пвх`). Разница между широкой и уточнённой частотностью и показывает, есть ли под фразой реальный спрос или он весь в её длинных хвостах.

Что ещё возвращает Вордстат: вложенные запросы (`SearchedWith` — что искали вместе с фразой, из них и собирается семантика с минус-словами) и похожие (`SearchedAlso` — соседние темы, источник новых групп и подсказка, какими словами аудитория называет продукт).

`shows: 0` — нулевая частотность в полученных данных, а не доказательство отсутствия
всего спроса. `shows: null` требует проверки `data_status`: `missing_response`
означает отсутствие ответа по фразе, `no_results` — пустую выдачу, `missing_frequency`
— отсутствие частотности самой фразы. Положительная частотность — `observed`,
нулевая — `zero`. Порог `min_shows` фильтрует подсказки, но сохраняет исходную
частотность. Рядом с TSV сохраняется `.meta.json` с датой, географией и SHA-256.
Числа Wordstat не являются прогнозом показов кампании и не складываются с
частотностью вложенных фраз. Низкая частотность сама по себе не исключает полезный ключ.

Дальше — группировать по намерению, предложению и посадочной, учитывая частотность,
выделять кандидатов в минус-слова. Правила количества, обязательные источники и
кандидаты нового плана: `direct://kb/structure-semantics`.
Для поиска учитывать коммерческий интент (купить, цена, заказать), а
информационные запросы оценивать по продукту и фактическому качеству обращений.
Для РСЯ — **базовые охватные фразы из 2–4 слов**, без низкочастотников:
там фраза задаёт тему, а не точное соответствие.

См. также: «Автотаргетинг, корректировки, ретаргетинг» (`direct://kb/targeting-adjustments`), «Плейбук донастройки» (`direct://kb/optimization-playbook`), «Чек-лист первичной настройки» (`direct://kb/launch-checklist`)

Источники: [Символы и операторы](https://yandex.ru/support/direct/ru/keywords/symbols-and-operators) · [Минус-слова и минус-фразы](https://yandex.ru/support/direct/keywords/negative-keywords.html) · [Ключевые фразы](https://yandex.ru/support/direct/ru/keywords/keywords) · [eLama: чек-лист аудита](https://elama.ru/blog/chek-list-audita-reklamy-v-yandeks-direkte/)


После аудита 11.09.2026 все ключи и минус-фразы проходят проверку символов и
парности операторов до первой записи. `б/у` отклоняется; варианты нужно записать
словами. Сложные ключи с `()`/`|` допускаются, варианты минус-фраз передаются отдельными
элементами. Executor и repair используют одну симметричную проверку: закрепление
известных стоп-слов (`своими`/`!своими`, `в`/`!в`) не создаёт ложного BLOCK.
Операторы у значимых слов, кавычки и содержательные отличия сохраняются.
План отмечает точные дубли между группами; для поисковых групп предлагает проверить
вложенные наборы слов и недостающую кросс-минусовку. Это ограниченная лексическая
проверка без морфологии, без автоматического изменения минусов.
